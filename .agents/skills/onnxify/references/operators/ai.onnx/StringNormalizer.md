# StringNormalizer

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `ai.onnx`
- Onnxify node type: `StringNormalizer`
- Since version: `10`
- Onnxify.TorchSharp converter coverage: `not detected`

## Description

StringNormalization performs string operations for basic cleaning.
This operator has only one input (denoted by X) and only one output
(denoted by Y). This operator first examines the elements in the X,
and removes elements specified in "stopwords" attribute.
After removing stop words, the intermediate result can be further lowercased,
uppercased, or just returned depending the "case_change_action" attribute.
This operator only accepts [C]- and [1, C]-tensor.
If all elements in X are dropped, the output will be the empty value of string tensor with shape [1]
if input shape is [C] and shape [1, 1] if input shape is [1, C].

## Onnxify Surface

- Input options type: `StringNormalizerInputOptions`
- Input/output options type: `StringNormalizerInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.StringNormalizer(string name, StringNormalizerInputOptions options) -> IOnnxGraphEdge`
  - `graph.StringNormalizer(string name, StringNormalizerInputOutputOptions options) -> IOnnxGraphEdge`

## Inputs

| Name | Onnxify property | Type | Allowed schema types | Semantics | Description |
| --- | --- | --- | --- | --- | --- |
| `X` | `X` | `IOnnxGraphEdge` | `tensor(string)` | single, required | UTF-8 strings to normalize |

## Outputs

| Name | Onnxify property | Type | Allowed schema types | Semantics | Description |
| --- | --- | --- | --- | --- | --- |
| `Y` | `Y` | `IOnnxGraphEdge` | `tensor(string)` | single, required | UTF-8 Normalized strings |

## Attributes

| Name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| `case_change_action` | `CaseChangeAction` | `string` | no | `NONE` | string enum that cases output to be lowercased/uppercases/unchanged. Valid values are "LOWER", "UPPER", "NONE". Default is "NONE" |
| `is_case_sensitive` | `IsCaseSensitive` | `long` | no | `0` | Boolean. Whether the identification of stop words in X is case-sensitive. Default is false |
| `locale` | `Locale` | `string` | no | `[null]` | Environment dependent string that denotes the locale according to which output strings needs to be upper/lowercased.Default en_US or platform specific equivalent as decided by the implementation. |
| `stopwords` | `Stopwords` | `string[]` | no | `[null]` | List of stop words. If not set, no word would be removed from X. |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.