# SequenceInsert

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `ai.onnx`
- Onnxify node type: `SequenceInsert`
- Since version: `11`
- Onnxify.TorchSharp converter coverage: `not detected`

## Description

Outputs a tensor sequence that inserts 'tensor' into 'input_sequence' at 'position'.
'tensor' must have the same data type as 'input_sequence'.
Accepted range for 'position' is in `[-n, n]`, where `n` is the number of tensors in 'input_sequence'.
Negative value means counting positions from the back.
'position' is optional, by default it inserts 'tensor' to the back of 'input_sequence'.

## Onnxify Surface

- Input options type: `SequenceInsertInputOptions`
- Input/output options type: `SequenceInsertInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.SequenceInsert(string name, SequenceInsertInputOptions options) -> IOnnxGraphEdge`
  - `graph.SequenceInsert(string name, SequenceInsertInputOutputOptions options) -> IOnnxGraphEdge`

## Inputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `input_sequence` | `InputSequence` | `IOnnxGraphEdge` | single, required | Input sequence. |
| `tensor` | `Tensor` | `IOnnxGraphEdge` | single, required | Input tensor to be inserted into the input sequence. |
| `position` | `Position` | `IOnnxGraphEdge` | optional | Position in the sequence where the new tensor is inserted. It is optional and default is to insert to the back of the sequence. Negative value means counting positions from the back. Accepted range in `[-n, n]`, where `n` is the number of tensors in 'input_sequence'. It is an error if any of the index values are out of bounds. It must be a scalar(tensor of empty shape). |

## Outputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `output_sequence` | `OutputSequence` | `IOnnxGraphEdge` | single, required | Output sequence that contains the inserted tensor at given position. |

## Attributes

| JSON name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| [none] |  |  |  |  |  |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.