# TreeEnsembleRegressor

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `ai.onnx.ml`
- Onnxify node type: `TreeEnsembleRegressor`
- Since version: `5`
- Onnxify.TorchSharp converter coverage: `not detected`

## Description

This operator is DEPRECATED. Please use TreeEnsemble instead which provides the same
    functionality.<br>
    Tree Ensemble regressor.  Returns the regressed values for each input in N.<br>
    All args with nodes_ are fields of a tuple of tree nodes, and
    it is assumed they are the same length, and an index i will decode the
    tuple across these inputs.  Each node id can appear only once
    for each tree id.<br>
    All fields prefixed with target_ are tuples of votes at the leaves.<br>
    A leaf may have multiple votes, where each vote is weighted by
    the associated target_weights index.<br>
    All fields ending with <i>_as_tensor</i> can be used instead of the
    same parameter without the suffix if the element type is double and not float.
    All trees must have their node ids start at 0 and increment by 1.<br>
    Mode enum is BRANCH_LEQ, BRANCH_LT, BRANCH_GTE, BRANCH_GT, BRANCH_EQ, BRANCH_NEQ, LEAF

## Onnxify Surface

- Input options type: `TreeEnsembleRegressorInputOptions`
- Input/output options type: `TreeEnsembleRegressorInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.ML.TreeEnsembleRegressor(string name, TreeEnsembleRegressorInputOptions options) -> IOnnxGraphEdge`
  - `graph.ML.TreeEnsembleRegressor(string name, TreeEnsembleRegressorInputOutputOptions options) -> IOnnxGraphEdge`

## Inputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `X` | `X` | `IOnnxGraphEdge` | single, required | Input of shape [N,F] |

## Outputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `Y` | `Y` | `IOnnxGraphEdge` | single, required | N classes |

## Attributes

| JSON name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| `aggregate_function` | `AggregateFunction` | `string` | no | `SUM` | Defines how to aggregate leaf values within a target. <br>One of 'AVERAGE,' 'SUM,' 'MIN,' 'MAX.' |
| `base_values` | `BaseValues` | `float[]` | no | `[null]` | Base values for regression, added to final prediction after applying aggregate_function; the size must be the same as the classes or can be left unassigned (assumed 0) |
| `base_values_as_tensor` | `BaseValuesAsTensor` | `OnnxTensor` | no | `[null]` | Base values for regression, added to final prediction after applying aggregate_function; the size must be the same as the classes or can be left unassigned (assumed 0) |
| `n_targets` | `NTargets` | `Nullable<long>` | no | `[null]` | The total number of targets. |
| `nodes_falsenodeids` | `NodesFalsenodeids` | `long[]` | no | `[null]` | Child node if expression is false |
| `nodes_featureids` | `NodesFeatureids` | `long[]` | no | `[null]` | Feature id for each node. |
| `nodes_hitrates` | `NodesHitrates` | `float[]` | no | `[null]` | Popularity of each node, used for performance and may be omitted. |
| `nodes_hitrates_as_tensor` | `NodesHitratesAsTensor` | `OnnxTensor` | no | `[null]` | Popularity of each node, used for performance and may be omitted. |
| `nodes_missing_value_tracks_true` | `NodesMissingValueTracksTrue` | `long[]` | no | `[null]` | For each node, define what to do in the presence of a NaN: use the 'true' (if the attribute value is 1) or 'false' (if the attribute value is 0) branch based on the value in this array.<br>This attribute may be left undefined and the default value is false (0) for all nodes. |
| `nodes_modes` | `NodesModes` | `string[]` | no | `[null]` | The node kind, that is, the comparison to make at the node. There is no comparison to make at a leaf node.<br>One of 'BRANCH_LEQ', 'BRANCH_LT', 'BRANCH_GTE', 'BRANCH_GT', 'BRANCH_EQ', 'BRANCH_NEQ', 'LEAF' |
| `nodes_nodeids` | `NodesNodeids` | `long[]` | no | `[null]` | Node id for each node. Node ids must restart at zero for each tree and increase sequentially. |
| `nodes_treeids` | `NodesTreeids` | `long[]` | no | `[null]` | Tree id for each node. |
| `nodes_truenodeids` | `NodesTruenodeids` | `long[]` | no | `[null]` | Child node if expression is true |
| `nodes_values` | `NodesValues` | `float[]` | no | `[null]` | Thresholds to do the splitting on for each node. |
| `nodes_values_as_tensor` | `NodesValuesAsTensor` | `OnnxTensor` | no | `[null]` | Thresholds to do the splitting on for each node. |
| `post_transform` | `PostTransform` | `string` | no | `NONE` | Indicates the transform to apply to the score. <br>One of 'NONE,' 'SOFTMAX,' 'LOGISTIC,' 'SOFTMAX_ZERO,' or 'PROBIT' |
| `target_ids` | `TargetIds` | `long[]` | no | `[null]` | The index of the target that each weight is for |
| `target_nodeids` | `TargetNodeids` | `long[]` | no | `[null]` | The node id of each weight |
| `target_treeids` | `TargetTreeids` | `long[]` | no | `[null]` | The id of the tree that each node is in. |
| `target_weights` | `TargetWeights` | `float[]` | no | `[null]` | The weight for each target |
| `target_weights_as_tensor` | `TargetWeightsAsTensor` | `OnnxTensor` | no | `[null]` | The weight for each target |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.