# OneHotEncoder

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `ai.onnx.ml`
- Onnxify node type: `OneHotEncoder`
- Since version: `1`
- Onnxify.TorchSharp converter coverage: `not detected`
- Onnxify.ModelGenerator TorchModule coverage: `not detected`

## Description

Replace each input element with an array of ones and zeros, where a single
    one is placed at the index of the category that was passed in. The total category count
    will determine the size of the extra dimension of the output array Y.<br>
    For example, if we pass a tensor with a single value of 4, and a category count of 8,
    the output will be a tensor with ``[0,0,0,0,1,0,0,0]``.<br>
    This operator assumes every input feature is from the same set of categories.<br>
    If the input is a tensor of float, int32, or double, the data will be cast
    to integers and the cats_int64s category list will be used for the lookups.

## Onnxify Surface

- Input options type: `OneHotEncoderInputOptions`
- Input/output options type: `OneHotEncoderInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.ML.OneHotEncoder(string name, OneHotEncoderInputOptions options) -> IOnnxGraphEdge`
  - `graph.ML.OneHotEncoder(string name, OneHotEncoderInputOutputOptions options) -> IOnnxGraphEdge`

## Inputs

| Name | Onnxify property | Type | Allowed schema types | Semantics | Description |
| --- | --- | --- | --- | --- | --- |
| `X` | `X` | `IOnnxGraphEdge` | `tensor(double)`<br>`tensor(float)`<br>`tensor(int32)`<br>`tensor(int64)`<br>`tensor(string)` | single, required | Data to be encoded. |

## Outputs

| Name | Onnxify property | Type | Allowed schema types | Semantics | Description |
| --- | --- | --- | --- | --- | --- |
| `Y` | `Y` | `IOnnxGraphEdge` | `tensor(float)` | single, required | Encoded output data, having one more dimension than X. |

## Attributes

| Name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| `cats_int64s` | `CatsInt64s` | `long[]` | no | `[null]` | List of categories, ints.<br>One and only one of the 'cats_*' attributes must be defined. |
| `cats_strings` | `CatsStrings` | `string[]` | no | `[null]` | List of categories, strings.<br>One and only one of the 'cats_*' attributes must be defined. |
| `zeros` | `Zeros` | `long` | no | `1` | If true and category is not present, will return all zeros; if false and a category if not found, the operator will fail. |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.

## ModelGenerator TorchModule Coverage

No `Onnxify.ModelGenerator` TorchModule operator that reconstructs this ONNX operator was detected via reflection.