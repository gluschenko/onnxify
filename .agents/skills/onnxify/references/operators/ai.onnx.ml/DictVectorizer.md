# DictVectorizer

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `ai.onnx.ml`
- Onnxify node type: `DictVectorizer`
- Since version: `1`
- Onnxify.TorchSharp converter coverage: `not detected`
- Onnxify.ModelGenerator TorchModule coverage: `not detected`

## Description

Uses an index mapping to convert a dictionary to an array.<br>
    Given a dictionary, each key is looked up in the vocabulary attribute corresponding to
    the key type. The index into the vocabulary array at which the key is found is then
    used to index the output 1-D tensor 'Y' and insert into it the value found in the dictionary 'X'.<br>
    The key type of the input map must correspond to the element type of the defined vocabulary attribute.
    Therefore, the output array will be equal in length to the index mapping vector parameter.
    All keys in the input dictionary must be present in the index mapping vector.
    For each item in the input dictionary, insert its value in the output array.
    Any keys not present in the input dictionary, will be zero in the output array.<br>
    For example: if the ``string_vocabulary`` parameter is set to ``["a", "c", "b", "z"]``,
    then an input of ``{"a": 4, "c": 8}`` will produce an output of ``[4, 8, 0, 0]``.

## Onnxify Surface

- Input options type: `DictVectorizerInputOptions`
- Input/output options type: `DictVectorizerInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.ML.DictVectorizer(string name, DictVectorizerInputOptions options) -> IOnnxGraphEdge`
  - `graph.ML.DictVectorizer(string name, DictVectorizerInputOutputOptions options) -> IOnnxGraphEdge`

## Inputs

| Name | Onnxify property | Type | Allowed schema types | Semantics | Description |
| --- | --- | --- | --- | --- | --- |
| `X` | `X` | `IOnnxGraphEdge` | `map(int64,tensor(double))`<br>`map(int64,tensor(float))`<br>`map(int64,tensor(string))`<br>`map(string,tensor(double))`<br>`map(string,tensor(float))`<br>`map(string,tensor(int64))` | single, required | A dictionary. |

## Outputs

| Name | Onnxify property | Type | Allowed schema types | Semantics | Description |
| --- | --- | --- | --- | --- | --- |
| `Y` | `Y` | `IOnnxGraphEdge` | `tensor(double)`<br>`tensor(float)`<br>`tensor(int64)`<br>`tensor(string)` | single, required | A 1-D tensor holding values from the input dictionary. |

## Attributes

| Name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| `int64_vocabulary` | `Int64Vocabulary` | `long[]` | no | `[null]` | An integer vocabulary array.<br>One and only one of the vocabularies must be defined. |
| `string_vocabulary` | `StringVocabulary` | `string[]` | no | `[null]` | A string vocabulary array.<br>One and only one of the vocabularies must be defined. |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.

## ModelGenerator TorchModule Coverage

No `Onnxify.ModelGenerator` TorchModule operator that reconstructs this ONNX operator was detected via reflection.