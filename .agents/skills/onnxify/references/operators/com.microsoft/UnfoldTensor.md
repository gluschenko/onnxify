# UnfoldTensor

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `com.microsoft`
- Onnxify node type: `UnfoldTensor`
- Since version: `1`
- Onnxify.TorchSharp converter coverage: `not detected`

## Description

Returns a tensor which contains all slices of size `size` from input tensor in the dimension `dim`. Step between two slices is given by `step`. If `sizedim` is the size of dimension `dim` for input tensor, the size of dimension `dim` in the returned tensor will be `(sizedim - size) / step + 1`. An additional dimension of size `size` is appended in the returned tensor.

## Onnxify Surface

- Input options type: `UnfoldTensorInputOptions`
- Input/output options type: `UnfoldTensorInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.Microsoft.UnfoldTensor(string name, UnfoldTensorInputOptions options) -> IOnnxGraphEdge`
  - `graph.Microsoft.UnfoldTensor(string name, UnfoldTensorInputOutputOptions options) -> IOnnxGraphEdge`

## Inputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `input` | `Input` | `IOnnxGraphEdge` | single, required | input tensor |

## Outputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `output` | `Output` | `IOnnxGraphEdge` | single, required | Output tensor. |

## Attributes

| JSON name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| `dim` | `Dim` | `long` | no | `-1` | specify the dimension to unfold |
| `size` | `Size` | `long` | yes | `[null]` | specify the size |
| `step` | `Step` | `long` | no | `1` | specify the step. |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.