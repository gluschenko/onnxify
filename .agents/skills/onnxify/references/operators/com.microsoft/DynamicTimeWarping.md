# DynamicTimeWarping

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `com.microsoft`
- Onnxify node type: `DynamicTimeWarping`
- Since version: `1`
- Onnxify.TorchSharp converter coverage: `not detected`

## Description

Input is cost matrix where each value in input[r][c] is the cost for pass the point (r, c). From current point(r, c),  points (r+1, c), (r+1, c+1) or (r, c+1) could be arrived in next move. Given such cost matrix, return dynamic time warping of shape [2, x], where the path made by all points (output[0][t], output[1][t])have the lowest cost among all paths from (0, 0) to (M-1, N-1).

## Onnxify Surface

- Input options type: `DynamicTimeWarpingInputOptions`
- Input/output options type: `DynamicTimeWarpingInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.Microsoft.DynamicTimeWarping(string name, DynamicTimeWarpingInputOptions options) -> IOnnxGraphEdge`
  - `graph.Microsoft.DynamicTimeWarping(string name, DynamicTimeWarpingInputOutputOptions options) -> IOnnxGraphEdge`

## Inputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `input` | `Input` | `IOnnxGraphEdge` | single, required | Input cost tensor, it must be 2D tensor of shape M x N, or 1 x M x N |

## Outputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `output` | `Output` | `IOnnxGraphEdge` | single, required | Output tensor. shape is [2, x], where max(M, N) <= x < M + N |

## Attributes

| JSON name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| [none] |  |  |  |  |  |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.