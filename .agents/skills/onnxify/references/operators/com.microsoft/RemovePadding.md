# RemovePadding

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `com.microsoft`
- Onnxify node type: `RemovePadding`
- Since version: `1`
- Onnxify.TorchSharp converter coverage: `not detected`

## Description

Compress transformer input by removing paddings. It assumes padding is on the right side of sequence.

The input has padding with shape (batch_size, sequence_length, hidden_size). This will generate two outputs:
output has shape (total_tokens, hidden_size); token_offset with shape (batch_size, sequence_length).

token_offset has offsets of all non-padding tokens first, then offset of all padding tokens. It is
a list of batch_size * sequence_length elements, which is reshaped to 2D for convenience of shape inference.

## Onnxify Surface

- Input options type: `RemovePaddingInputOptions`
- Input/output options type: `RemovePaddingInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.Microsoft.RemovePadding(string name, RemovePaddingInputOptions options) -> RemovePaddingOutput`
  - `graph.Microsoft.RemovePadding(string name, RemovePaddingInputOutputOptions options) -> RemovePaddingOutput`

## Inputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `input` | `Input` | `IOnnxGraphEdge` | single, required | Input tensor with shape (batch_size, sequence_length, hidden_size) |
| `sequence_token_count` | `SequenceTokenCount` | `IOnnxGraphEdge` | single, required | Number of non-padding tokens in each sequence with shape (batch_size). |

## Outputs

| JSON name | Onnxify property | Type | Semantics | Description |
| --- | --- | --- | --- | --- |
| `output` | `Output` | `IOnnxGraphEdge` | single, required | output tensor with shape (total_tokens, hidden_size) |
| `token_offset` | `TokenOffset` | `IOnnxGraphEdge` | single, required | Offset of non-padding tokens, and those of padding tokens. Its shape is (batch_size, sequence_length) |
| `cumulated_seq_len` | `CumulatedSeqLen` | `IOnnxGraphEdge` | single, required | Cumulated sequence lengths. Its shape is (batch_size + 1) |
| `max_seq_len` | `MaxSeqLen` | `IOnnxGraphEdge` | single, required | Max sequence length without padding. Its shape is (1) |

## Attributes

| JSON name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| [none] |  |  |  |  |  |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.