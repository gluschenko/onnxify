# InplaceClipGradNorm

[Back to operator index](../index.md)

Autogenerated by `src/Onnxify.AgentSkillGenerator`.

- Domain: `com.microsoft`
- Onnxify node type: `InplaceClipGradNorm`
- Since version: `1`
- Onnxify.TorchSharp converter coverage: `not detected`
- Onnxify.ModelGenerator TorchModule coverage: `not detected`

## Description

InplaceClipGradNorm operator, taking multiple gradients as inputs (seq<tensor>). InplaceClipGradNorm should be used in conjunction with optimizers that accept seq<tensor> gradients as input, since this op takes a sequence of tensors as input and outputs a sequence of tensors there by avoiding the need for SequenceConstruct (and making any unnecessary copy).Please note that the gradient clipping happens inplace.

## Onnxify Surface

- Input options type: `InplaceClipGradNormInputOptions`
- Input/output options type: `InplaceClipGradNormInputOutputOptions`
- Wrapper overloads detected via reflection:
  - `graph.Microsoft.InplaceClipGradNorm(string name, InplaceClipGradNormInputOptions options) -> IOnnxGraphEdge`
  - `graph.Microsoft.InplaceClipGradNorm(string name, InplaceClipGradNormInputOutputOptions options) -> IOnnxGraphEdge`

## Inputs

| Name | Onnxify property | Type | Allowed schema types | Semantics | Description |
| --- | --- | --- | --- | --- | --- |
| `gradients` | `Gradients` | `IOnnxGraphEdge` | `seq(tensor(double))`<br>`seq(tensor(float))`<br>`seq(tensor(float16))` | single, required | Sequence of gradients computed in this iteration. |

## Outputs

| Name | Onnxify property | Type | Allowed schema types | Semantics | Description |
| --- | --- | --- | --- | --- | --- |
| `clipped_gradients` | `ClippedGradients` | `IOnnxGraphEdge` | `seq(tensor(double))`<br>`seq(tensor(float))`<br>`seq(tensor(float16))` | single, required | Gradients after being clipped as per given inputs and attributes. |

## Attributes

| Name | Onnxify property | Type | Required | Default | Description |
| --- | --- | --- | --- | --- | --- |
| `max_norm` | `MaxNorm` | `float` | no | `1.0` | Coefficient of previously accumulated gradient in running average. |
| `norm_type` | `NormType` | `string` | no | `fro` | Type of normalization to perform during execution of clip grad norm.Currently, the only norm supported is the frobenius norm (which is also the default). |

## TorchSharp Coverage

No `Onnxify.TorchSharp` converter path that emits this operator was detected via reflection and IL analysis.

## ModelGenerator TorchModule Coverage

No `Onnxify.ModelGenerator` TorchModule operator that reconstructs this ONNX operator was detected via reflection.